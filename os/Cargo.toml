[package]
name = "os"
version = "0.1.0"
edition = "2024"

[dependencies]
riscv = { version = "0.15.0" }
lazy_static = { version = "1.4.0", features = ["spin_no_std"] }
buddy_system_allocator = "0.6"
bitflags = "1.2.1"
xmas-elf = "0.7.0"

[features]
board_qemu = []
board_k210 = []

[profile.dev]
panic = "abort"
debug = true

[profile.release]
panic = "abort"
debug = 1  # 或 2 以包含更多调试信息

[package.metadata.lints.rust]
missing_docs = "allow"